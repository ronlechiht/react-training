export interface CartProduct {
  productId: string;
  productName: string;
  productPrice: number;
  productDiscount: number;
  productColor: string;
  productSize: string;
  productQuantity: number;
}
